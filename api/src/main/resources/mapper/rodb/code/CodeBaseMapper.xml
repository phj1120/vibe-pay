<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.api.app.repository.code.CodeBaseMapper">

    <!-- 공통코드 전체 조회 -->
    <select id="selectAllCodeBase" resultType="CodeBase">
        SELECT /* CodeBaseMapper.selectAllCodeBase */
               GROUP_CODE
             , GROUP_CODE_NAME
          FROM CODE_BASE
         ORDER BY GROUP_CODE
    </select>

    <!-- 그룹코드로 조회 -->
    <select id="selectCodeBaseByGroupCode" parameterType="string" resultType="CodeBase">
        SELECT /* CodeBaseMapper.selectCodeBaseByGroupCode */
               GROUP_CODE
             , GROUP_CODE_NAME
          FROM CODE_BASE
         WHERE GROUP_CODE = #{groupCode}
    </select>

</mapper>
