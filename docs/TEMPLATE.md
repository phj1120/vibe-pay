# [번호]. [모듈명]

### 개발 요청사항

[핵심 요구사항 및 구현 방식 명시]

예시:
- 특정 디자인 패턴 적용 (전략 패턴, 팩토리 패턴 등)
- 주요 코드 값 및 ENUM 정보
- 공통코드 기반 개발 지침

### 현재 구현의 제한 사항

[고려하지 않을 사항들을 명확히 나열]

예시:
- XXX 관련 업무는 현재 고려하지 않을 것.
- YYY에 대한 동시성 문제 고려하지 않을 것.
- ZZZ 관련 기능 고려하지 않을 것.

### 설정 값

application.yml 또는 application.properties

```yaml
# 외부 API 연동 설정
module:
  api-key: "키값"
  endpoint: "엔드포인트"
  timeout: 3000
```

또는

```properties
# 설정 예시
module.api-key=키값
module.endpoint=엔드포인트
module.timeout=3000
```

### 주요 프로세스

```yaml
(화면/외부) 이벤트 발생
	→ (서버) API 엔드포인트 `/api/module/action`
	→ 데이터 검증 및 처리
	→ 외부 시스템 연동 (필요시)
		→ 요청 생성
		→ 응답 처리
		→ 에러 핸들링
	→ DB 트랜잭션 처리
	→ 후처리 작업
	→ (화면) 결과 반환

**성공/실패 결과 처리:**
- 성공 → 성공 화면 또는 다음 단계
- 실패 → 에러 모달 (상세 정보 포함) → 이전 화면 또는 대체 흐름
```

주요 규칙:
- [규칙 1]
- [규칙 2]
- [규칙 3]

### 프론트엔드 구현 세부사항 (해당시)

```json
#### 1. 주요 흐름

**파일 구조:**
- `/path/to/page` - 메인 페이지
- `/path/to/component` - 주요 컴포넌트

**통신 흐름:**

단계별 설명
  ↓ 액션
다음 단계
  ↓ 액션
결과

#### 2. 데이터 구조

interface MainDataType {
  field1: string;
  field2: number;
  field3?: OptionalType;
}

#### 3. 에러 메시지 형식

**에러 케이스 1:**
에러 메시지

[상세 정보]
항목: 값
발생 시각: YYYY. MM. DD. 오전/오후 HH:MM:SS

**에러 케이스 2:**
에러 메시지 및 대응 방안

#### 4. 상태 관리

- **초기 상태**: 설명
- **처리 중**: 설명
- **완료**: 설명
- **에러**: 설명
```

### 외부 시스템 연동 (해당시)

- 시스템명 A
    - 요청 정보

        | name | 매핑 필드 | 기타 |
        | --- | --- | --- |
        | fieldName1 | sourceField1 | 필수 |
        | fieldName2 | sourceField2 | 선택 |

    - 응답 정보
        
        | name | 설명 | 비고 |
        | --- | --- | --- |
        | responseField1 | 응답필드1 설명 | 성공코드: XXX |
        | responseField2 | 응답필드2 설명 |  |

- 시스템명 B
    - 요청 방식
        - POST/GET {엔드포인트}
        - Request

            | 필드명 | 타입 | 설명 | 필수여부 |
            | --- | --- | --- | --- |
            | field1 | String | 설명 | 필수 |
            | field2 | Number | 설명 | 선택 |

        - Response
            
            | 필드명 | 타입 | 설명 |
            | --- | --- | --- |
            | resultCode | String | 결과코드 (성공: 0000) |
            | resultMsg | String | 결과메시지 |

### API 정의서

- API명 1
    - METHOD `/api/path/to/endpoint`
    - Request: `RequestDtoName`

        | 타입 | 변수명 | 설명 |
        | --- | --- | --- |
        | String | field1 | 필드 설명 |
        | Long | field2 | 필드 설명 |
        | List\<SubDto\> | field3 | 서브 객체 리스트 |

    - Response: `ResponseDtoName`
        
        | 타입 | 변수명 | 설명 | 필수여부 |
        | --- | --- | --- | --- |
        | String | field1 | 필드 설명 | 필수 |
        | Long | field2 | 필드 설명 | 선택 |

    - 프로세스
        
        ```
        1. 요청 데이터 검증
           - 검증1: 설명
           - 검증2: 설명
        
        2. 비즈니스 로직 처리
           - 처리1: 설명
           - 처리2: 설명
        
        3. DB 작업
           - 테이블A insert/update/delete
           - 테이블B insert/update/delete
        
        4. 후처리
           - 작업1: 설명
           - 작업2: 설명
        ```

- API명 2
    - METHOD `/api/path/to/endpoint2`
    - Request: X (파라미터 없음)
    - Response

        | 타입 | 변수명 | 설명 |
        | --- | --- | --- |
        | String | field1 | 설명 |

    - 프로세스
        
        ```
        처리 로직 설명
        ```

### 데이터 흐름도 (DFD)

예시를 위한 데이터

- 테이블명 A

    | 컬럼1 | 컬럼2 | 컬럼3 |
    | --- | --- | --- |
    | 값1 | 값2 | 값3 |

---

- 테이블명 B

    | 컬럼1 | 컬럼2 | 컬럼3 |
    | --- | --- | --- |
    | 값1 | 값2 | 값3 |

---

- 시나리오 1: [시나리오 설명]
    - [단계 1]
        - 테이블명 A
            
            | 컬럼1 | 컬럼2 | 컬럼3 |
            | --- | --- | --- |
            | 값1 | 값2 | 값3 |

        ---

        - 테이블명 B
            
            | 컬럼1 | 컬럼2 | 컬럼3 |
            | --- | --- | --- |
            | 값1 | 값2 | 값3 |

    - [단계 2]
        - 테이블명 A
            
            | 컬럼1 | 컬럼2 | 컬럼3 | 변경사항 |
            | --- | --- | --- | --- |
            | 값1 | 값2 | 값3 | 기존 |
            | 값4 | 값5 | 값6 | 신규 추가 |

        ---

        - 테이블명 B
            
            | 컬럼1 | 컬럼2 | 컬럼3 | 변경사항 |
            | --- | --- | --- | --- |
            | 값1 | 값2_수정 | 값3 | 컬럼2 업데이트 |

- 시나리오 2: [시나리오 설명]
    - [단계 설명]
        - 테이블별 데이터 변화 기록

### 특이사항 및 주의사항

- **주의사항 1**: 설명
- **주의사항 2**: 설명
- **특이사항**: 설명

### 에러 처리 전략

1. **검증 에러**
   - 케이스: 설명
   - 처리: 예외 던지기, 롤백 여부

2. **외부 시스템 연동 에러**
   - 케이스: 설명
   - 처리: 재시도, 보상 트랜잭션, 로깅

3. **DB 트랜잭션 에러**
   - 케이스: 설명
   - 처리: 롤백 범위, 보상 처리

### 테스트 시나리오

1. **정상 케이스**
   - 입력: 설명
   - 기대 결과: 설명

2. **예외 케이스 1**
   - 입력: 설명
   - 기대 결과: 설명

3. **예외 케이스 2**
   - 입력: 설명
   - 기대 결과: 설명

### 참고사항

- 관련 문서: [링크]
- 참고 코드: [경로]
- 기타 메모

