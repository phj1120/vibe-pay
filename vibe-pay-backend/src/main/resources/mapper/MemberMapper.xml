<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vibe.pay.backend.member.MemberMapper">

    <resultMap id="memberResultMap" type="com.vibe.pay.backend.member.Member">
        <id property="memberId" column="member_id"/>
        <result property="name" column="name"/>
        <result property="shippingAddress" column="shipping_address"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="email" column="email"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findAll" resultMap="memberResultMap">
        SELECT member_id, name, shipping_address, phone_number, email, created_at FROM member
    </select>

    <select id="findByMemberId" resultMap="memberResultMap">
        SELECT member_id, name, shipping_address, phone_number, email, created_at FROM member WHERE member_id = #{memberId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO member (name, shipping_address, phone_number, email, created_at) VALUES (#{name}, #{shippingAddress}, #{phoneNumber}, #{email}, #{createdAt})
    </insert>

    <update id="update">
        UPDATE member SET name = #{name}, shipping_address = #{shippingAddress}, phone_number = #{phoneNumber}, email = #{email} WHERE member_id = #{memberId}
    </update>

    <delete id="delete">
        DELETE FROM member WHERE member_id = #{memberId}
    </delete>

</mapper>